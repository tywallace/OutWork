<% if logged_in? %>
  <div class="container">
    <div>
      <%= render 'shared/patch_notes' %>
    </div>
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
    <div class="main-profile">
      <div class="row">
        <div class = "timer_profile">
          <section class="user_info">
            <%= render 'shared/user_info' %>
          </section>
          <section class="stats">
            <%= render 'shared/stats' %>
          </section>
          <br>
            <button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#patchModal">
              Patch Notes
            </button>

        </div>
   <!--  <section class="micropost_form">
                <%= render 'shared/micropost_form' %>
               </section> -->


          <div class = "col-md-12">
            <% if todays_goal(current_user) == "Not Active Today" %>  
              <div class="goal_form">
                <%= render 'shared/goal_form' %>
              </div>
          <% else %>

              <div class = "counter">
                <div id = "personal_tracker">
                <%= render 'shared/personal_tracker' %>
                </div>
              </div>
            </div>
          <% end %>
        <%= render 'timer/timer' %>
      </div>
      I apologize from the weird behavior with Pomodoros today. There are issues with the default timezone (which is 4 hours off from Eastern Standard Time). I am working to correct this and hopefully will have something by tomorrow.
    <br><br>
      Your Pomodoro's are not lost. This is just an error with how I am displaying them.
    </div>



  <!--       <div class="col-md-8">
          <h3>Micropost Feed</h3>
          <%= render 'shared/feed' %> 
        </div> --> 
    
    <div class = "row">
      <div class = "col-md-8">
        <div class="scoreboard-area">
          <div id="scoreboard">
            <%= render 'shared/scoreboard' %>
          </div>
        </div>
      </div>
      <div class = "col-md-4">
        <div class="scoreboard-area">
          <div id="leaderboard">
            <%= render 'shared/leaderboard' %>
          </div>
        </div>
      </div>
    </div>
</div>
<% else %>
  <%= render 'layouts/landing' %>
<% end %>