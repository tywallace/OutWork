<% provide(:title, "Edit user") %>

<div class="container">
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
	
	<h1>Update your profile (Disabled)</h1>

	<div class="row">
	  <div class="col-md-6 col-md-offset-3">
	  </div>
	</div>
</div>